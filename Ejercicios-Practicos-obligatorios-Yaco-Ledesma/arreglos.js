function nuevoArreglo(numero) {
    var arreglo = [];
    for (var i = 1; i <= numero; i++) {
        arreglo.push(i);
    }
    return arreglo;
}
console.log(nuevoArreglo(x));
  // Cambiar la x del console.log para mostrar el resultado, o declarar una variable a x, funcionara de las dos maneras